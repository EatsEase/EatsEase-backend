## How to Run the Project

1. Clone the repository:
   ```bash
   git clone
    ```
2. Install npm dependencies:
    ```bash
    npm install
    ```
3. Run the application:
    ```bash
    npm run start
    ```
